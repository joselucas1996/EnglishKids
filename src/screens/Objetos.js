import React, { Component} from 'react';
import { View, Text, StyleSheet, Image, TouchableOpacity } from'react-native';

export default class Objetos extends Component{
    render(){
        return(
            <View>
                <Text>Tela de Objetos</Text>
            </View>
        );
    }
}