import React, { Component} from 'react';
import { View, Text, StyleSheet, Image, TouchableOpacity } from'react-native';

export default class Alfabeto extends Component{
    render(){
        return(
            <View>
                <Text>Tela do Alfabeto</Text>
            </View>
        );
    }
}