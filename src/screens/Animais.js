import React, { Component} from 'react';
import { View, Text, StyleSheet, Image, TouchableOpacity } from'react-native';

export default class Animas extends Component{
    render(){
        return(
            <View>
                <Text>Tela de Animais
                </Text>
            </View>
        );
    }
}