import React, { Component} from 'react';
import { View, Text, StyleSheet, Image, TouchableOpacity } from'react-native';

export default class Cores extends Component{
    render(){
        return(
            <View>
                <Text>Tela de Cores</Text>
            </View>
        );
    }
}